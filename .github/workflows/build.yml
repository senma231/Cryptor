name: 打包多平台可执行文件

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write
  packages: read

jobs:
  build-windows:
    name: 打包 Windows 可执行文件
    runs-on: windows-latest
    
    steps:
    - name: 检出代码
      uses: actions/checkout@v4
    
    - name: 设置 Python 环境
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        cache: 'pip'
    
    - name: 安装依赖
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller
    
    - name: 运行 PyInstaller 打包
      run: |
        pyinstaller --clean `
          --name="量化交易系统" `
          --windowed `
          --onefile `
          --icon=icon.ico `
          --add-data "icon.png;." `
          --add-data "backtest_worker.py;." `
          --add-data "download_worker.py;." `
          --add-data "paper_trading_worker.py;." `
          --add-data "config;config" `
          --add-data "strategies;strategies" `
          --add-data "tools;tools" `
          --hidden-import=PyQt5 `
          --hidden-import=pandas `
          --hidden-import=numpy `
          --hidden-import=cryptography `
          --hidden-import=ccxt `
          --hidden-import=requests `
          --collect-all=PyQt5 `
          --collect-all=pandas `
          --collect-all=numpy `
          trading_gui.py
      shell: pwsh
    
    - name: 重命名可执行文件为英文
      run: |
        mv "dist/量化交易系统.exe" "dist/Cryptor-Trading-System.exe"

    - name: 上传 Windows 可执行文件
      uses: actions/upload-artifact@v4
      with:
        name: windows-executable
        path: dist/Cryptor-Trading-System.exe
        retention-days: 30

  # macOS Intel 构建已被 GitHub 弃用，暂时禁用
  # build-macos-intel:
  #   name: 打包 macOS Intel 可执行文件
  #   runs-on: macos-13

  build-macos-arm:
    name: 打包 macOS Apple Silicon 可执行文件
    runs-on: macos-latest  # Apple Silicon Mac

    steps:
    - name: 检出代码
      uses: actions/checkout@v4

    - name: 设置 Python 环境
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        cache: 'pip'

    - name: 安装依赖
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller

    - name: 运行 PyInstaller 打包
      run: |
        pyinstaller --clean \
          --name="量化交易系统" \
          --windowed \
          --onefile \
          --icon=icon.ico \
          --add-data "icon.png:." \
          --add-data "backtest_worker.py:." \
          --add-data "download_worker.py:." \
          --add-data "paper_trading_worker.py:." \
          --add-data "config:config" \
          --add-data "strategies:strategies" \
          --add-data "tools:tools" \
          --hidden-import=PyQt5 \
          --hidden-import=pandas \
          --hidden-import=numpy \
          --hidden-import=cryptography \
          --hidden-import=ccxt \
          --hidden-import=requests \
          --collect-all=PyQt5 \
          --collect-all=pandas \
          --collect-all=numpy \
          trading_gui.py

    - name: 创建 .app 包
      run: |
        mkdir -p "量化交易系统.app/Contents/MacOS"
        mkdir -p "量化交易系统.app/Contents/Resources"
        cp dist/量化交易系统 "量化交易系统.app/Contents/MacOS/"
        cp icon.png "量化交易系统.app/Contents/Resources/"

        cat > "量化交易系统.app/Contents/Info.plist" << EOF
        <?xml version="1.0" encoding="UTF-8"?>
        <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
        <plist version="1.0">
        <dict>
            <key>CFBundleExecutable</key>
            <string>量化交易系统</string>
            <key>CFBundleIconFile</key>
            <string>icon.png</string>
            <key>CFBundleIdentifier</key>
            <string>com.cryptor.trading</string>
            <key>CFBundleName</key>
            <string>量化交易系统</string>
            <key>CFBundlePackageType</key>
            <string>APPL</string>
            <key>CFBundleShortVersionString</key>
            <string>2.0.0</string>
        </dict>
        </plist>
        EOF

        chmod +x "量化交易系统.app/Contents/MacOS/量化交易系统"
        zip -r "Cryptor-Trading-System-macOS-ARM.zip" "量化交易系统.app"

    - name: 上传 macOS ARM 可执行文件
      uses: actions/upload-artifact@v4
      with:
        name: macos-arm-executable
        path: Cryptor-Trading-System-macOS-ARM.zip
        retention-days: 30

  create-release:
    name: 创建 GitHub Release
    needs: [build-windows, build-macos-arm]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write

    steps:
    - name: 下载所有 artifacts
      uses: actions/download-artifact@v4
      with:
        path: artifacts

    - name: 显示下载的文件
      run: |
        echo "下载的 artifacts:"
        ls -R artifacts/

    - name: 重命名文件为中文显示名称
      run: |
        mv artifacts/windows-executable/Cryptor-Trading-System.exe "artifacts/windows-executable/量化交易系统.exe"
        mv artifacts/macos-arm-executable/Cryptor-Trading-System-macOS-ARM.zip "artifacts/macos-arm-executable/量化交易系统-macOS-ARM.zip"

    - name: 创建 Release 并上传文件
      uses: softprops/action-gh-release@v2
      with:
        name: "量化交易系统 ${{ github.ref_name }}"
        body: |
          # 量化交易系统 ${{ github.ref_name }} (Pre-release)

          ## 🎉 预发布版本

          ### ✨ 主要功能
          - 📥 多交易所数据下载（Binance、OKX、HTX）
          - 📊 策略回测功能
          - 🎮 模拟交易功能
          - 📋 策略管理功能
          - 🔐 策略加密保护
          - 📈 支持 13 种 K 线周期（1m, 3m, 5m, 15m, 30m, 1h, 2h, 4h, 6h, 12h, 1d, 1w, 1M）

          ### 📦 下载说明
          1. 根据你的系统下载对应的文件：
             - **Windows**: `量化交易系统.exe`
             - **macOS (Apple Silicon)**: `量化交易系统-macOS-ARM.zip`
          2. Windows: 直接运行 `.exe` 文件
          3. macOS: 解压 `.zip` 后运行 `.app` 文件

          ### ⚠️ 注意事项
          - 这是预发布版本，可能存在未知问题
          - 请在测试环境中使用
          - 欢迎反馈问题和建议

          ---

          **⚠️ 免责声明**: 本软件仅供学习和研究使用。加密货币交易具有高风险，请谨慎使用。
        files: |
          artifacts/windows-executable/量化交易系统.exe
          artifacts/macos-arm-executable/量化交易系统-macOS-ARM.zip
        prerelease: true
        token: ${{ secrets.GITHUB_TOKEN }}

